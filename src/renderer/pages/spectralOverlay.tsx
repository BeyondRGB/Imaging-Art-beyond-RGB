import React from 'react';
import LinkButton from '../components/linkButton';

export default function SpectralOverlay(): JSX.Element {
  return (
    <div className="LaunchPage">
      <LinkButton text="SpectralOverlay" linkTo="/" className="start-button"/>
    </div>
  )
}